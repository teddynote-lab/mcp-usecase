```
.
├── rag/
│   ├── __init__.py     # 패키지 초기화
│   ├── base.py         # 추상 기본 클래스 (템플릿 메서드 패턴)
│   └── pdf.py          # PDF 구현체
├── mcp_auto_json.py    # 서버 실행에 필요한 JSON 파일 내용 생성
├── mcp_server.py       # MCP 도구 정의
└── config.py           # 설정 파일

```